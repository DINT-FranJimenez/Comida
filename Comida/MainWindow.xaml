<Window x:Class="Comida.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Comida"
        mc:Ignorable="d"
        Title="MainWindow" MinHeight="500" Height="700" MinWidth="600" Width="950" Background="LightSkyBlue">
    <Border Background="White"  BorderThickness="1" BorderBrush="Black" Margin="18">
        <DockPanel LastChildFill="True" Margin="10,10,10,5">
            
            <StackPanel DockPanel.Dock="Right" Background="LightGray" Width="280">
                <TextBlock Text="Nombre del plato" Margin="10,10,0,0"/>
                <TextBox IsEnabled="False" Margin="10,0,10,0"/>
                <TextBlock Text="Foto del plato" Margin="10,10,0,0"/>
                <TextBox IsEnabled="False" Margin="10,0,10,0"/>
                <TextBlock Text="Tipo del plato" Margin="10,10,0,0"/>
                <ComboBox IsEnabled="False" Margin="10,0,10,5"/>
                <GroupBox Header="Alérgenos" BorderBrush="Black" Margin="10,0,10,0">
                    <UniformGrid>
                        <CheckBox Content="Leche" Margin="5" IsEnabled="False"/>
                        <CheckBox Content="Soja" Margin="5" IsEnabled="False"/>
                        <CheckBox Content="Gluten" Margin="5" IsEnabled="False"/>
                        <CheckBox Content="Sulfitos" Margin="5" IsEnabled="False"/>
                    </UniformGrid>
                </GroupBox>
                <Button Content="Limpiar Selección" Width="125" HorizontalAlignment="Left"
                        Margin="10,10,10,0" IsEnabled="False"/>
            </StackPanel>

            <Border BorderBrush="Gray" BorderThickness="1"  Margin="0,0,15,0">
                <ScrollViewer  VerticalScrollBarVisibility ="Auto"
                  HorizontalScrollBarVisibility ="Auto">
                    <WrapPanel>
                        
                    </WrapPanel>
                </ScrollViewer>
            </Border>
        </DockPanel>
    </Border>
</Window>
